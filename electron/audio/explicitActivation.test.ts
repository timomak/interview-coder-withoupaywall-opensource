// P09-AC1 entrypoint: the owned runtime suite proves that startup/cleanup do
// not construct the helper and only an explicit source action can do so.
import "./native/NativeAudioCaptureRuntime.test"
